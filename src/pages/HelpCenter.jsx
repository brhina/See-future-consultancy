import ResourcePage from '../components/ResourcePage'
import { helpCenterPageData } from '../data/resourcePagesData'

function HelpCenter() {
  return <ResourcePage pageData={helpCenterPageData} />
}

export default HelpCenter
